@using GC_CoffeeLab24b.Models;
@{
    ViewBag.Title = "Add";
}

<h2>Book Added</h2>

Cart:
<ul>
    @foreach (Item b in (List<Item>)(Session["Cart"]))
    {
        <li>@b.ItemID byte @b.ItemName</li>
    }
</ul>

